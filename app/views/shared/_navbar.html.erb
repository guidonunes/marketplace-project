<div class="navbar navbar-lewagon">
  <div class="container">
    <div class="d-flex align-items-center w-50">
      <%= link_to root_path, class: "navbar-brand d-flex" do %>
        <%= image_tag "logo.jpeg" %>Bushido</p>
      <% end %>
      <%= render "shared/search" %>
    </div>

    <div class="d-flex">
      <% if user_signed_in? %>
        <div class="nav-item active">
          <%= link_to "Categories", "#", class: "nav-link" %>
        </div>

        <div class="nav-item active ms-4">
          <%= link_to "My Products", my_products_products_path, class: "nav-link" %>
        </div>

        <div class="nav-item active ms-4">
          <%= link_to "Log out", destroy_user_session_path, data: {turbo_method: :delete}, class: "nav-link" %>
        </div>
      <% else %>
          <div class="nav-item">
            <%= link_to "Login", new_user_session_path, class: "nav-link" %>
          </div>
      <% end  %>
    </div>
  </div>
</div>
